
    // Triángulo más ancho y bajo (cuello y hombros)
    const neckWidth = size - 60;  // Aumentamos la base aún más
    const neckHeight = size / 4; // Aumentamos la altura para hacerlo más grande

    // Ajustamos la posición para que el triángulo de "cuello y hombros" empiece después del triángulo inferior
    const newOffsetY = quarter + offsetY + size / 4;  // Aquí sumamos un poco más de espacio

    this.drawTriangle(
      (-neckWidth / 2) * scale + 64, (newOffsetY - neckHeight) * scale,  // Punta superior (hacia arriba)
      (-neckWidth / 2) * scale, newOffsetY * scale,                    // Esquina izquierda
      (neckWidth / 2) * scale, newOffsetY * scale,                     // Esquina derecha
      this.colors.orange  // Puedes usar otro color si lo prefieres
    );

    // ---- CREACIÓN DEL CUERPO ----
    // Definimos el cuerpo como un trapezoide debajo de los hombros
    const bodyWidthTop = neckWidth;         // La parte superior del cuerpo coincide con los hombros
    const bodyWidthBottom = size;           // La base del cuerpo es más ancha
    const bodyHeight = size / 2;            // Altura del cuerpo
    const bodyTopY = newOffsetY;            // Comienza donde terminan los hombros
    const bodyBottomY = newOffsetY-50 + bodyHeight; // Extiende hacia abajo

    // Dibujar el cuerpo como un trapezoide usando dos triángulos
    this.drawTriangle(
      (-bodyWidthTop / 2) * scale, bodyTopY * scale,         // Hombro izquierdo
      (bodyWidthTop / 2) * scale, bodyTopY * scale,          // Hombro derecho
      (bodyWidthBottom / 2) * scale, bodyBottomY * scale * 1.85,    // Cadera derecha
      this.colors.teal
    );

    // Segundo triángulo: conecta el hombro izquierdo, la cadera derecha y la cadera izquierda
    this.drawTriangle(
      (-bodyWidthTop / 2) * scale, bodyTopY * scale,         // Hombro izquierdo
      (bodyWidthBottom / 2) * scale, bodyBottomY * scale* 1.85,    // Cadera derecha
      (-bodyWidthBottom / 2) * scale, bodyBottomY * scale * 1.95,   // Cadera izquierda
      this.colors.purple
    );
    
